{
  "name": "sm-toolkit",
  "private": true,
  "workspaces": [
    "packages/*",
    "cli"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}

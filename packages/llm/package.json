{
  "name": "@smokingmouse/llm",
  "version": "0.5.0",
  "type": "module",
  "description": "配置驱动的 LLM 直连 API 客户端 —— 读 endpoints.yaml（$SM_ENDPOINTS_PATH / ~/.config/sm/endpoints.yaml），OpenAI/Anthropic 兼容 provider + retry。",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SmokingMouse/sm-toolkit.git",
    "directory": "packages/llm"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "endpoints.example.yaml"
  ],
  "scripts": {
    "prepare": "tsc --build",
    "build": "tsc --build"
  },
  "dependencies": {
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.2"
  }
}

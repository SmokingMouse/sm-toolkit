{
  "name": "@sm/agent",
  "version": "0.2.0",
  "type": "module",
  "description": "Claude Code / Codex CLI 编排引擎 —— 统一 Backend 抽象(吸收自 agent-gateway),原生接入 @sm/llm 的 endpoints.yaml 做模型/端点解析。",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc --build",
    "build": "tsc --build"
  },
  "dependencies": {
    "@sm/llm": "file:../llm"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.2"
  }
}

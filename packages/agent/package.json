{
  "name": "@smokingmouse/agent",
  "version": "0.3.0",
  "type": "module",
  "description": "Claude Code / Codex CLI 编排引擎 —— 统一 Backend 抽象(吸收自 agent-gateway),原生接入 @smokingmouse/llm 的 endpoints.yaml 做模型/端点解析。",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SmokingMouse/sm-toolkit.git",
    "directory": "packages/agent"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc --build",
    "build": "tsc --build"
  },
  "dependencies": {
    "@smokingmouse/llm": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.2"
  }
}

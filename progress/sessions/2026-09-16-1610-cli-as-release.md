# 2026-09-16 16:10 · llm --as 接 agent-server 合入并发版（trellis leader 编队，PR #20）

## 做了什么
- `apps/cli` 加 as/1 客户端 + Codex TUI 启动器：`llm <model> --as` 在常驻 daemon 建 backend=claude 线程再 exec 官方 TUI resume；默认路线 / -p / bench 未动；`--print-launch` dry-run、`LLM_ROUTE` 设默认、`--permission` 四档。
- 环境类失败回落本地并 stderr 一行；策略类拒绝（model_denied / model_required）报错 exit 2 不回落。
- 31 单测绿、tsc 0；tmux pty 真机一轮 + 四情形硬验收（trellis `.fenjue/briefs/cli-as-bridge/verify.sh`）；PR #20 → 0458284；发版 `@smokingmouse/cli@0.6.0`（c11e0dd）。

## 决定
- [decision] 方案 A；否决 B（TUI 自建线程：isClaudeModel 把网关限定名全判成 Codex 引擎）与 C（-p 走 as/1 一句 ok 烧 16k tokens）。调研见 trellis `.fenjue/archive/fj-cli-as-bridge-0b2e/`。

## Next
可选：thread/start.effort 补回 effort；cpa provider 级 claude.env 钉 tier 模型；systemError 线程 close 报 busy（failures.md 待查）。

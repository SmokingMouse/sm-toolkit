# 任意 item 分叉（fj-as-midfork-4778）

- 已实现 AS 前缀快照、独立 turn ID / seq、forkedFrom 与非法坐标校验。
- 验证：`bun test packages/agent-server/src/core/thread-manager.test.ts`：16 pass。
- Claude 原生完整 turn 坐标用 `--resume-session-at`；其余播种 `shouldQuery:false` 用户 + assistant stream-json，未续聊关闭时重播种。
- 验证：`bun test packages/agent-server/src/engines/claude.test.ts`：46 pass；`bun run typecheck`：exit 0。
- Codex 原生 `thread/fork + lastTurnId`；任意 item 用角色标注历史文本 `developerInstructions` 播种，AS item 不额外增添上下文消息。
- 验证：`bun test packages/agent-server/src/engines/codex.test.ts`：64 pass。
- 协议表格/类型块与 schema 已同步；新增可选 midThreadFork 和 AgentClient.fork，记录播种成本与保真度限制。
- 验证：`bun test packages/agent-server/src/client/client.test.ts packages/agent-server/src/protocol`：51 pass。
- 跨引擎 core 回归：全 item 变体、含尾前缀、源未变、seq、独立续聊、原生坐标跨重启、播种恢复；修复恢复时旧状态覆盖问题（见 failures.md）。
- 收尾审查修复无坐标 tip 的并发追加竞态；缺省 item 也只有精确坐标才走原生，否则播种。定向 core 25 pass，包含新增竞态回归。
- 根目录新增 alignment 命令转发到既有 package checker。
- 最终全量 proof：`bun run typecheck` exit 0；`cd packages/agent-server && bun test` 291 pass；`cd apps/agent-tui && bun test` 122 pass / 4 snapshots；`bun scripts/check-codex-alignment.ts` 32 contracts aligned（Codex 0.153.4）。TUI 空源 fork 断言同步为快照语义，其余流程不变。
- 与 23cc581 的 schema 递归比较：既有字段、required 列表和值不变，仅新增可选字段；`git diff --check` exit 0。
- 证据与交付：`/Users/smokingmouse/python/learning/trellis/.fenjue/tasks/fj-as-midfork-4778/out/`；仅 fake child / MockEngine，未起真引擎、未 push。
- Next：主控独立验收；真实引擎保真度与恢复复验另单，Trellis 迁移 Goal 不在本契约内改状态。

# 上线交接 fj-smtk-ship-main-3d07

已按仓内模板生成用户 LaunchAgent，固定主仓 dist、Bun 绝对路径与 ~/.agent-server/launchd.{out,err}.log；plist lint 通过。配置和 policy 未改。

可复跑切换、生产双后端 PTY 冒烟与验收证据位于 `/Users/smokingmouse/python/learning/trellis/.fenjue/tasks/fj-smtk-ship-main-3d07/out/`。切换脚本在坐席检查和 SQLite 备份后才停止旧 daemon；重启会断开本席，由 leader 执行。

初始阻塞：远端缺 feat/codex-ingress，契约禁止 push；主仓已有未提交改动。已发信箱请求 leader 推送并保全主仓改动。Next：以上线 result.md 的 PR、构建、D1–D4 实测结果及后续 switch/smoke 证据为准，不将脚本准备视为生产上线完成。
